# 更新日誌

本文件記錄「待辦清單」App 各版本的功能變化。

## [0.2.0] - Step 2

### 新增
- 深色模式切換按鈕,可在淺色 / 深色之間手動切換
- 深色模式偏好會存進 `localStorage`,重新整理頁面後維持
- 若使用者從未手動切換過,深色模式會跟隨作業系統的 `prefers-color-scheme` 設定
- 篩選功能:新增「全部」「未完成」「已完成」三個篩選按鈕

### 變更
- 深色配色沿用既有 CSS 變數機制,於 `[data-theme="dark"]` 下覆寫顏色變數
- 篩選後清單為空時,提示文字會依篩選條件顯示對應內容
- 底部「未完成:N 項」維持顯示整體數量,不受篩選影響

## [0.1.0] - Step 1

### 新增
- 輸入框與新增按鈕,可新增待辦事項
- 每筆待辦左側勾選框,勾選後文字加上刪除線並淡化
- 每筆待辦右側刪除按鈕,可刪除該筆待辦
- 底部顯示「未完成:N 項」
- 清單為空時顯示提示文字「還沒有任何待辦事項,新增一個吧!」
- 資料存入 `localStorage`,重新整理頁面後資料仍保留
- 輸入空白內容時不會新增待辦事項

### 變更
- 版面置中,最大寬度 520px,採卡片式設計、圓角與淡陰影
- 支援手機螢幕的響應式版面(RWD)
- 顏色皆使用定義在 `:root` 的 CSS 變數
